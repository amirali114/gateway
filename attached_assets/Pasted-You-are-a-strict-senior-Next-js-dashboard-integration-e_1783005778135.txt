You are a strict senior Next.js dashboard integration engineer.

PROJECT:
Unixsee Campaign Gateway

BASE ZIP:
unixsee_campaign_gateway-r10.3-controlled-beta-staging-hardening.zip

DESIGN REFERENCE ZIP:
Unixsee-Gateway-Dashboard.zip

CREATE:
unixsee_campaign_gateway-r10.6-dashboard-replit-ui-integrated.zip

GOAL:
Use the BASE project as the only runtime source of truth.
Use the DESIGN REFERENCE only as a visual/UI reference.
Replace the current dashboard visual layer with the Replit dashboard design, while preserving all BASE runtime, auth, RBAC, security, routes, and Mother API behavior.

ABSOLUTE RULES:
- Do NOT change Mother Go code.
- Do NOT change Agent Go code.
- Do NOT change PHP Gateway runtime.
- Do NOT enable enforcement.
- Do NOT add remote commands.
- Do NOT expose Agent.
- Do NOT expose Mother token to browser.
- Do NOT change auth/session/token logic.
- Do NOT change RBAC semantics.
- Do NOT change storage schema.
- Do NOT remove existing routes.
- Do NOT import Replit api-server, db, .git, .local, pnpm store, Vite runtime, or mock backend.
- Do NOT use Google CDN fonts.
- Dashboard must remain Persian RTL.
- Dashboard must use server-side Mother API only.
- Browser must never fetch Mother directly.
- Browser must never receive management token.

BASE RUNTIME TO PRESERVE:
- app/login/actions.ts
- app/logout/route.ts
- existing auth/session guard
- existing RBAC permission model
- existing server-only Mother API client
- existing release gates integration
- existing users/audit/settings routes
- existing .env behavior
- existing local-only staging behavior

DESIGN TASK:
Port the visual structure from Unixsee-Gateway-Dashboard.zip into the BASE Next.js App Router dashboard.

Use the Replit design as inspiration for:
- DashboardShell
- Sidebar
- Topbar
- KPI cards
- Agent cards
- Release panels
- Status pills
- Tables
- Empty states
- RawJsonDrawer

But convert everything to BASE Next.js App Router structure.

Do NOT keep:
- Vite
- wouter
- App.tsx
- main.tsx
- index.html
- client-side fake routing
- fake API server

ROUTES TO KEEP:
/
 /login
 /logout
 /agents
 /agents/[agent_id]
 /release
 /mother
 /diagnostics
 /gateway
 /policy
 /alerts
 /users
 /audit
 /settings
 /settings/production
 /sync

DATA RULE:
UI must read normalized view models from server-side adapters.
Do not let UI components call Mother directly.
Use existing Mother API client on the server side only.

REAL API SOURCES:
GET /healthz
GET /readyz
GET /v1/storage/status
GET /v1/health/report
GET /v1/release-gates
GET /v1/release-gates/summary
GET /v1/agents
GET /v1/policies/default
GET /v1/alerts/summary

CONTENT CORRECTION:
Remove all unrelated AI/inference text from the Replit UI:
- inference-worker
- embedding-service
- GPU
- model-manager
- guardrail-validator
- canary deployment

Replace with:
- Gateway Agent
- Mother Core
- PHP Gateway runtime
- shadow-only telemetry
- policy sync
- release gates
- storage status
- local-only safety
- no enforcement
- no remote command

ACTIONS:
Remove or disable unsupported actions:
- continue rollout
- pause rollout
- rollback
- save production changes
- destructive actions not backed by current safe APIs

RAW JSON:
Raw JSON must only appear inside RawJsonDrawer/collapsible details.
Main screens must show polished cards/tables/summaries.

ACCEPTANCE:
- npm ci passes.
- npm run build passes.
- /login returns 200.
- all routes render.
- auth/RBAC still works.
- no actual secret value in client bundle.
- no management token sent to browser.
- Mother base URL remains server-side.
- Dashboard remains local-only behind SSH tunnel.

OUTPUT:
unixsee_campaign_gateway-r10.6-dashboard-replit-ui-integrated.zip