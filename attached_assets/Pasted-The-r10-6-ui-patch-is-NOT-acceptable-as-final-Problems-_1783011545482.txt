The r10.6-ui-patch is NOT acceptable as final.

Problems:
- It only ships src/ patch files, not a full build-ready release.
- server adapters still throw errors.
- auth/RBAC guard is not wired.
- login form does not use existing loginAction and uses email instead of username.
- logout uses POST but BASE logout route is GET.
- Tailwind classes are used but BASE project has no Tailwind setup.
- next/font/google is not acceptable because it requires build-time Google access.
- @/ alias is used but BASE tsconfig does not define it.
- production/settings page still contains static fake production values.

Create a full release ZIP:

CREATE:
unixsee_campaign_gateway-r10.6-dashboard-replit-ui-integrated.zip

BASE:
unixsee_campaign_gateway-r10.3-controlled-beta-staging-hardening.zip

DESIGN REFERENCE:
Unixsee-Gateway-Dashboard.zip

RULES:
- Output must be a full Unixsee Campaign Gateway release ZIP, not a patch.
- npm ci must pass.
- npm run build must pass.
- Do not leave any throw new Error stubs.
- Do not leave TODOs in runtime paths.
- Do not change Mother Go.
- Do not change Agent Go.
- Do not change PHP Gateway runtime.
- Do not change auth/session/token logic.
- Do not change RBAC semantics.
- Do not expose Mother token to browser.
- Browser must never fetch Mother directly.
- Keep app/login/actions.ts logic.
- Login page must use existing loginAction.
- Login field names must remain username/password.
- Keep app/logout/route.ts behavior or update UI to use GET logout link.
- Every protected page must keep requirePermission().
- Dashboard layout must keep requireDashboardAuth().
- Sidebar must filter nav items by auth.permissions.
- Use existing server-only lib/api.ts Mother client.
- Map real Mother APIs into UI view models.
- No Google CDN and no next/font/google.
- If Tailwind is used, include complete Tailwind/PostCSS/package config and ensure generated CSS works.
- Prefer no Tailwind unless fully configured.
- No raw JSON on main screens; raw JSON only in drawer/collapse.
- No fake production values unless clearly labeled unavailable.
- No unsupported write/destructive buttons.

Acceptance:
- npm ci passes.
- npm run build passes.
- grep -R "throw new Error\|TODO: Replace\|action=\"/login\"\|next/font/google\|@tailwind" dashboard must return no runtime blockers.
- /login returns 200.
- /, /agents, /release, /mother, /diagnostics, /settings/production render after login.
- No actual secret appears in client bundle.