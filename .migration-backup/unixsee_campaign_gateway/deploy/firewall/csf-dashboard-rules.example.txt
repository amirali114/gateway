# Unixsee Gateway staging firewall guidance for CSF.
# Examples only. Review before applying on a live server.

# Public web ports for HTTPS reverse proxy:
# TCP_IN should include 80,443 only as required by the public web server.

# Do NOT expose Dashboard backend port:
# 8740 must bind to 127.0.0.1 only and must not be in TCP_IN.

# Do NOT expose Agent port:
# 8731 must bind to 127.0.0.1 only on the client server and must not be public.

# Mother API port:
# 8732 should bind to 127.0.0.1 by default.
# If remote Agents must reach Mother directly, bind Mother explicitly and allow only trusted Agent source IPs.
# Example concept, not an instruction to paste blindly:
#   tcp|in|d=8732|s=<TRUSTED_AGENT_OR_PROXY_IP>

# Iran-access/proxy scenario:
# If the Iran Agent reaches Germany Mother through a proxy or fixed egress IP, allow that egress IP only.
# Keep HMAC signatures enabled for Agent-to-Mother traffic.
